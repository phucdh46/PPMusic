package com.dhp.musicplayer.repository

import javax.inject.Inject
import javax.inject.Singleton

@Singleton
class LoginsRepositoryImpl @Inject constructor(

): LoginsRepository {
    override fun getUser() {

    }
}